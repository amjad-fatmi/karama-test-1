# The built website lives in site/ (generated by src/build.py and committed).
[build]
  publish = "site"
  command = ""

# Don't expose the internal route report.
[[redirects]]
  from = "/_routes.txt"
  to = "/404.html"
  status = 404
